class Solution:
    def reductionOperations(self, nums: List[int]) -> int:
        n = len(nums)
        count = 0
        up = 0

        nums.sort()
        for i in range(1, n):
            if nums[i] != nums[i-1]:
                up += 1
            count += up
        return count