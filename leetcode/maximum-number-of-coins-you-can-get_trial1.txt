class Solution:
    def maxCoins(self, piles: List[int]) -> int:
        piles.sort()
        max_coins = 0
        n = len(piles)//3

        for i in range(n, len(piles), 2):
            max_coins += piles[i]

        return max_coins