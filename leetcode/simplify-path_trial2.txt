class Solution:
    def simplifyPath(self, path: str) -> str:
        #using list as a stack
        res = []

        for p in path.split('/'):
            if p == '..':
                if res:
                    res.pop()
            elif p and p != '.':
                res.append(p)

        return '/' + '/'.join(res)        